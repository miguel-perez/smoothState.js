/**
 * Caching
 * @todo never fetch or cache form submissions
 * @todo POST never times out
 * @todo bad prefetches dont reload the page
 * @todo setting cacheLength to 0 disables caching
 * @todo prefetches on defined events
 * @todo allows you to define the cache
 */

$(function() {
  'use strict';

  QUnit.smoothStateModule('caching');

});
